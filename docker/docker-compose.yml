version: "3"
services:
  vehicle-inventory-app:
    container_name: vehicle-inventory-app 
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    restart: always
